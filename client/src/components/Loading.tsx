import React from 'react'

const Loading = () => {
  return (
    <div className='w-full flex items-center justify-center h-screen bg-background font-semibold font-mono'>Loading ...</div>
  )
}

export default Loading